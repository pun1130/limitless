plugins {
    id("wheel-fabric").version("latest.integration")
}

group = "user11681"
version = "1.2.2"

loom {
    accessWidener("src/main/resources/limitless.accesswidener")
}

repositories {
    maven {name = "jitpack"}
}

dependencies {
    modInclude("api-base")
    modInclude("api-resource-loader")
    modInclude("api-tag-extensions:1.0.3+")

    //modInclude("common-formatting") requires phormat; Bintray
    //modInclude("huntingham-hills") Bintray

    modInclude("reflect")

    bloatedInclude("auto-config")
    bloatedInclude("cloth-config")

    apiInclude("shortcode")

    //mod("phormat") // required for overlines but no longer available due to Bintray's shutdown

    intransitive("mod-menu")

    mod("api:0.29.4+1.16")

    /* Charm
    api("toml4j")
    api("com.electronwill.night-config:toml:latest.release")
    api("com.electronwill.night-config:core:latest.release")
    mod("com.github.svenhjol:Charm:master-SNAPSHOT")
    */

    mod("curse.maven:moenchantments-320806:3084973") // one horrible mess of a mod
    //mod("com.github.Spottedleaf:Starlight:fabric-SNAPSHOT") // ASM transformer breaks Starlight's serialization due to byte -> int conversion
}
